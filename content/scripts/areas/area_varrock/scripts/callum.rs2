[opnpc1,callum]
if (map_feature("customshops") = false) {
    ~chatnpc("<p,neutral>Sorry, I'm not currently trading.");
    return;
}
~chatnpc("<p,happy>Welcome! I sell rare items.");
def_int $option = ~p_choice2("Let me take a look.", 1, "No thanks.", 2);
if($option = 1) {
    ~chatplayer("<p,happy>Let me take a look.");
    ~openshop_activenpc;
} else {
    ~chatplayer("<p,neutral>No thanks.");
}

[ai_queue3,callum]
if (map_feature("customshops") = true) {
    npc_add(0_51_52_19_58, callum, 2000000);
}
